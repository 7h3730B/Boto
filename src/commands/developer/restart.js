module.exports.info = {
    name: "restart",
    description: "cmds.developer.restart.description",
    dm: true,
    permission: 10,
}

module.exports.run = async (client, message, args) => {
    
}